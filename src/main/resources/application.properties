spring.application.name=ResidentEase

spring.datasource.url=jdbc:mysql://localhost:3306/society_management_db
spring.datasource.username=root
spring.datasource.password=Pr@S@d#2025!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# use JDBC-backed sessions (OR remove this if you want in-memory)
spring.session.store-type=jdbc
# set to always first time; change to 'never' once tables exist
spring.session.jdbc.initialize-schema=always

# session timeout
server.servlet.session.timeout=30m

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=prasadghadge59@gmail.com
spring.mail.password=uvzfspdoljtjavsq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

